# SSH keys
*.pem
*.pub

# Terraform secrets and generated state
terraform/voteball.tfvars
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform.tfstate.lock.info
terraform/.terraform/
terraform/snapshot.auto.tfvars

# EKS stack (Plan 2) — same posture as the k3s terraform/ stack above.
terraform-eks/voteball-eks.tfvars
terraform-eks/terraform.tfstate
terraform-eks/terraform.tfstate.backup
terraform-eks/.terraform.tfstate.lock.info
terraform-eks/tfplan
terraform-eks/.terraform/

# Ansible: secrets.yml is committed ENCRYPTED via ansible-vault — only the
# vault password itself and the generated (non-secret) inventory stay out of git.
ansible-project/.vault_pass
ansible-project/inventories/voteball/hosts
ansible-project/inventories/voteball/group_vars/all/main.yml

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# subagent-driven-development scratch (progress ledger, review packages)
.superpowers/

# Personal plain-language explainer (not part of the submission)
EXPLAINER.md
