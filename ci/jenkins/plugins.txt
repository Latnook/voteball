# Top-level plugins only -- jenkins-plugin-cli resolves dependencies itself, so this file stays
# readable and there is exactly one place to justify a plugin's existence.
#
# Comments are on their own lines, never trailing a plugin id: jenkins-plugin-cli parses each
# non-comment line as `artifact-id[:version]`, and a trailing `# why` would be read as part of it.
#
# Versions are DELIBERATELY unpinned. This host is rebuilt rarely and always wants the versions
# matching the Jenkins LTS it boots with; pinning here would mean a second, silently-stale list to
# maintain alongside the Jenkins version itself. The trade-off that used to apply here is gone: these
# plugins are now baked into an immutable ECR image, so the IMAGE TAG is the pin. Two deploys of the
# same image tag are byte-identical even though this file names no versions. `jenkins-plugin-cli
# --list` output is captured at image build.
#
# Replaces the 94 plugins the setup wizard's "install suggested" left behind. Every entry below is
# traceable to something the Jenkinsfile or this host actually does.

# --- JCasC itself ---
# reads ci/jenkins/jenkins.yaml; without it this whole directory is inert
configuration-as-code
# the `jobs:` block in jenkins.yaml is Job DSL
job-dsl

# --- what the Jenkinsfile uses ---
# declarative pipeline (pipeline, stages, when, post)
workflow-aggregator
# SCM checkout
git
# triggers { githubPush() } + the webhook shared-secret config
github
# sshagent(credentials: ['voteball-deploy-key']) -- G4, the tag-bump push
ssh-agent
# options { timestamps() }
timestamper
# ephemeral pod agents -- the whole reason this host is now in the cluster
kubernetes

# Deliberately NOT included:
#   docker-workflow  -- there is no Docker daemon and no `docker` CLI in a pod agent at all. Builds
#                       use rootless BuildKit via buildctl; see the Jenkinsfile.
#   matrix-auth      -- authorization is loggedInUsersCanDoAnything; a single admin needs no matrix.
#   email-ext/mailer -- no SMTP on this host (G7). Adding them would imply notifications that do not
#                       exist. See docs/production-readiness.md section 7.
#   github-branch-source / workflow-multibranch -- this project commits directly to master and uses
#                       a single Pipeline job, not a multibranch one.
