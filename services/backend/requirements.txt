flask==3.1.3
gunicorn==23.0.0
psycopg2-binary==2.9.11
# Both are Flask transitive deps, but this app imports them DIRECTLY -- werkzeug.security for the
# admin password hash, itsdangerous for the signed session token. Declared explicitly so a future
# Flask release that drops or renames either fails at install time, not at login time.
werkzeug==3.1.8
itsdangerous==2.2.0
